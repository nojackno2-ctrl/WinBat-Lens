<Window x:Class="WinBatLens.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinBat Lens - Windows 電池健康度與即時硬體耗電監測" 
        Height="960" Width="1380" MinHeight="800" MinWidth="1060"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgDark}"
        Foreground="{StaticResource TextMain}"
        Icon="pack://application:,,,/app_icon.png">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header Bar -->
        <Border Grid.Row="0" Background="{StaticResource BgCard}" 
                BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                CornerRadius="14" Padding="18,14" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="40" Height="40" CornerRadius="10" Background="Transparent" Margin="0,0,12,0">
                        <Image Source="pack://application:,,,/app_icon.png" Width="38" Height="38"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="TxtHeaderAppTitle" Text="WinBat Lens" FontSize="19" FontWeight="Bold" Foreground="{StaticResource TextMain}"/>
                        <TextBlock x:Name="TxtSystemModel" Text="Windows 電池健康診斷與全系統耗電監測" FontSize="12" Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Language Switcher Button -->
                    <Button x:Name="BtnLanguageToggle" Content="🌐 English" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,12,0" Padding="10,5" Click="BtnLanguageToggle_Click"/>

                    <!-- Auto Startup Toggle CheckBox -->
                    <CheckBox x:Name="ChkAutoStart" Content="🚀 開機自動啟動" Foreground="{StaticResource TextMain}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0" Cursor="Hand" Click="ChkAutoStart_Click"/>

                    <Button x:Name="BtnOpenReport" Content="📁 開啟 HTML 報告" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Click="BtnOpenReport_Click"/>
                    <Button x:Name="BtnGenerateReport" Content="⚡ 執行電池檢測" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" Click="BtnGenerateReport_Click"/>
                    <Button x:Name="BtnExportData" Content="📥 匯出 JSON" Style="{StaticResource SecondaryButtonStyle}" Click="BtnExportData_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Dashboard Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="370"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT PANEL: Scrollable Clean Sidebar -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,16,0" Padding="0,0,8,10">
                <StackPanel>

                    <!-- Health Gauge Card -->
                    <Border Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderColor}" 
                            BorderThickness="1" CornerRadius="12" Padding="16,14" Margin="0,0,0,14">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="TxtHealthCardTitle" Text="電池健康度 (Health Score)" FontSize="12" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                            
                            <!-- Health Score Ring / Value -->
                            <Grid Margin="0,10">
                                <Ellipse Width="115" Height="115" Stroke="#1E293B" StrokeThickness="11"/>
                                <Ellipse x:Name="RingHealthProgress" Width="115" Height="115" Stroke="{StaticResource AccentEmerald}" StrokeThickness="11" StrokeDashArray="360" StrokeDashOffset="360"/>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock x:Name="TxtHealthPercent" Text="--" FontSize="34" FontWeight="ExtraBold" Foreground="{StaticResource TextMain}"/>
                                        <TextBlock x:Name="TxtHealthPercentSign" Text="%" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextMuted}" VerticalAlignment="Bottom" Margin="2,0,0,6"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>

                            <!-- Status Badge -->
                            <Border x:Name="BadgeStatus" CornerRadius="12" Background="#2010B981" BorderBrush="{StaticResource AccentEmerald}" BorderThickness="1" Padding="12,4" HorizontalAlignment="Center">
                                <TextBlock x:Name="TxtStatusLabel" Text="--" FontWeight="Bold" FontSize="12" Foreground="{StaticResource AccentEmerald}"/>
                            </Border>

                            <TextBlock x:Name="TxtSummary" Text="請點擊右上角「執行電池檢測」按鈕載入最新報告。" 
                                       FontSize="11" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Battery Specs Card -->
                    <Border Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderColor}" 
                            BorderThickness="1" CornerRadius="12" Padding="16,14" Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock x:Name="TxtBatterySpecsTitle" Text="🔋 電池規格參數" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextMain}" Margin="0,0,0,12"/>
                            
                            <Grid Margin="0,3">
                                <TextBlock x:Name="LblSpecName" Text="電池名稱" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                <TextBlock x:Name="TxtSpecName" Text="--" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#10FFFFFF" Margin="0,5"/>

                            <Grid Margin="0,3">
                                <TextBlock x:Name="LblSpecMfg" Text="製造商" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                <TextBlock x:Name="TxtSpecMfg" Text="--" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#10FFFFFF" Margin="0,5"/>

                            <Grid Margin="0,3">
                                <TextBlock x:Name="LblSpecChem" Text="化學材質" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                <TextBlock x:Name="TxtSpecChem" Text="--" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#10FFFFFF" Margin="0,5"/>

                            <Grid Margin="0,3">
                                <TextBlock x:Name="LblSpecDesign" Text="設計容量" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                <TextBlock x:Name="TxtSpecDesign" Text="-- mWh" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#10FFFFFF" Margin="0,5"/>

                            <Grid Margin="0,3">
                                <TextBlock x:Name="LblSpecFull" Text="滿電容量" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                <TextBlock x:Name="TxtSpecFull" Text="-- mWh" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#10FFFFFF" Margin="0,5"/>

                            <Grid Margin="0,3">
                                <TextBlock x:Name="LblSpecLoss" Text="容量損耗" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                <TextBlock x:Name="TxtSpecLoss" Text="-- mWh" Foreground="{StaticResource AccentRose}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#10FFFFFF" Margin="0,5"/>

                            <Grid Margin="0,3">
                                <TextBlock x:Name="LblSpecCycles" Text="充放電循環" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                <TextBlock x:Name="TxtSpecCycles" Text="-- 次" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Capacity Degradation History Card (Optimized 370px Proportional Table) -->
                    <Border Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderColor}" 
                            BorderThickness="1" CornerRadius="12" Padding="14,12" Margin="0,0,0,10">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <TextBlock x:Name="TxtCapacityHistoryTitle" Text="📈 容量歷史紀錄" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextMain}"/>
                                <TextBlock x:Name="TxtReportTime" Text="尚未載入" FontSize="10" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Table Header Row -->
                            <Border Background="#101B2E" CornerRadius="6" Padding="8,6" Margin="0,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="85"/>
                                        <ColumnDefinition Width="65"/>
                                        <ColumnDefinition Width="65"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="TxtColPeriod" Text="期間" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Grid.Column="0"/>
                                    <TextBlock x:Name="TxtColFullCap" Text="滿電" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Grid.Column="1" HorizontalAlignment="Right" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="TxtColDesignCap" Text="設計" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Grid.Column="2" HorizontalAlignment="Right" Margin="0,0,10,0"/>
                                    <TextBlock x:Name="TxtColHealthPct" Text="健康%" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Grid.Column="3" HorizontalAlignment="Right" Margin="0,0,6,0"/>
                                </Grid>
                            </Border>

                            <!-- Table Data Rows -->
                            <ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <ItemsControl x:Name="LvCapacityHistory">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#0C1322" BorderBrush="#182338" BorderThickness="1" CornerRadius="6" Padding="8,6" Margin="0,0,0,4">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="85"/>
                                                        <ColumnDefinition Width="65"/>
                                                        <ColumnDefinition Width="65"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Period}" FontSize="11" Foreground="{StaticResource TextMain}" VerticalAlignment="Center" Grid.Column="0"/>
                                                    <TextBlock Text="{Binding FullChargeCapacity}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource AccentCyan}" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="1" Margin="0,0,6,0"/>
                                                    <TextBlock Text="{Binding DesignCapacity}" FontSize="11" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="2" Margin="0,0,10,0"/>
                                                    <Border Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Center" CornerRadius="8" Padding="5,2" Background="#2010B981" BorderBrush="{StaticResource AccentEmerald}" BorderThickness="1">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="{Binding HealthPercent}" FontSize="10" FontWeight="Bold" Foreground="{StaticResource AccentEmerald}"/>
                                                            <TextBlock Text="%" FontSize="9" Foreground="{StaticResource AccentEmerald}"/>
                                                        </StackPanel>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- RIGHT PANEL: Full-Height Real-Time Monitoring Dashboard -->
            <Border Grid.Column="1" Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderColor}" 
                    BorderThickness="1" CornerRadius="14" Padding="20">
                <TabControl x:Name="TabMainControl" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextMain}">
                    
                    <!-- TAB 1: Full System Real-Time Power Monitor -->
                    <TabItem x:Name="TabRealTime" Header=" ⚡ 全系統硬體功耗分佈 ">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                            <StackPanel>
                                <!-- Power Discharge & Time Cards -->
                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Live Discharge Rate Card -->
                                    <Border Grid.Column="0" Background="#182338" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,8,0">
                                        <StackPanel>
                                            <TextBlock x:Name="TxtCardTotalPower" Text="⚡ 電池總放電 / AC 變壓器總供電功率" FontSize="13" Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock x:Name="TxtLiveDischargeRate" Text="-- W" FontSize="30" FontWeight="ExtraBold" Foreground="{StaticResource AccentCyan}" Margin="0,6"/>
                                            <Border x:Name="BadgeLiveAcState" Background="#2038BDF8" BorderBrush="{StaticResource AccentCyan}" BorderThickness="1" CornerRadius="12" Padding="10,3" HorizontalAlignment="Left">
                                                <TextBlock x:Name="TxtLiveAcState" Text="電源狀態讀取中..." FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource AccentCyan}"/>
                                            </Border>
                                        </StackPanel>
                                    </Border>

                                    <!-- Live Remaining Battery Time Card -->
                                    <Border Grid.Column="1" Background="#182338" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="8,0,0,0">
                                        <StackPanel>
                                            <TextBlock x:Name="TxtCardEstTime" Text="⏱️ 電腦估算剩餘續航時間" FontSize="13" Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock x:Name="TxtLiveRemainingTime" Text="--" FontSize="26" FontWeight="Bold" Foreground="{StaticResource AccentEmerald}" Margin="0,8"/>
                                            <TextBlock x:Name="TxtLiveBatteryPercent" Text="目前電量: --%" FontSize="13" Foreground="{StaticResource TextMuted}"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <!-- Task Manager Style Real-Time Waveform Chart Card -->
                                <Border Background="#182338" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,16">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,10">
                                            <TextBlock x:Name="TxtWaveformTitle" Text="📈 60 秒即時動態走勢波形圖 (Task Manager Style Live Graph)" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextMain}"/>
                                            
                                            <!-- Legend -->
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                <Border Width="12" Height="3" Background="{StaticResource AccentCyan}" Margin="0,0,4,0"/>
                                                <TextBlock x:Name="TxtLegendDischarge" Text="放電 (W)" FontSize="11" Foreground="{StaticResource AccentCyan}" Margin="0,0,10,0"/>

                                                <Border Width="12" Height="3" Background="{StaticResource AccentEmerald}" Margin="0,0,4,0"/>
                                                <TextBlock x:Name="TxtLegendCharge" Text="充電 (+W)" FontSize="11" Foreground="{StaticResource AccentEmerald}" Margin="0,0,10,0"/>

                                                <Border Width="12" Height="3" Background="{StaticResource AccentPurple}" Margin="0,0,4,0"/>
                                                <TextBlock x:Name="TxtLegendCpu" Text="CPU (%)" FontSize="11" Foreground="{StaticResource AccentPurple}" Margin="0,0,10,0"/>
                                                
                                                <Border Width="12" Height="3" Background="{StaticResource AccentAmber}" Margin="0,0,4,0"/>
                                                <TextBlock x:Name="TxtLegendGpu" Text="獨顯 (%)" FontSize="11" Foreground="{StaticResource AccentAmber}"/>
                                            </StackPanel>
                                        </Grid>

                                        <!-- Waveform Canvas Box with Y-Axis Wattage Coordinates -->
                                        <Border Height="160" Background="#060910" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="6" ClipToBounds="True">
                                            <Grid x:Name="GridChartContainer" SizeChanged="GridChartContainer_SizeChanged">
                                                <!-- Background Gridlines -->
                                                <Canvas x:Name="CanvasGridlines"/>
                                                
                                                <!-- Y-Axis Wattage Scale Labels Overlay (Left Side) -->
                                                <Grid VerticalAlignment="Stretch" HorizontalAlignment="Left" Margin="8,4,0,4" IsHitTestVisible="False">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    
                                                    <TextBlock x:Name="TxtYAxis100" Grid.Row="0" Text="35 W (100%)" FontSize="10" Foreground="#94A3B8" FontFamily="Consolas, Segoe UI" VerticalAlignment="Top"/>
                                                    <TextBlock x:Name="TxtYAxis75" Grid.Row="1" Text="26 W (75%)" FontSize="10" Foreground="#94A3B8" FontFamily="Consolas, Segoe UI" VerticalAlignment="Top"/>
                                                    <TextBlock x:Name="TxtYAxis50" Grid.Row="2" Text="18 W (50%)" FontSize="10" Foreground="#94A3B8" FontFamily="Consolas, Segoe UI" VerticalAlignment="Top"/>
                                                    <TextBlock x:Name="TxtYAxis25" Grid.Row="3" Text="9 W (25%)" FontSize="10" Foreground="#94A3B8" FontFamily="Consolas, Segoe UI" VerticalAlignment="Top"/>
                                                    <TextBlock x:Name="TxtYAxis0" Grid.Row="4" Text="0 W (0%)" FontSize="10" Foreground="#94A3B8" FontFamily="Consolas, Segoe UI" VerticalAlignment="Bottom"/>
                                                </Grid>

                                                <!-- Separate Polylines for Discharge (Cyan), Charge (Emerald), CPU (Purple), GPU (Amber) -->
                                                <Polyline x:Name="PolylineDischarge" Stroke="{StaticResource AccentCyan}" StrokeThickness="2" StrokeLineJoin="Round"/>
                                                <Polyline x:Name="PolylineCharge" Stroke="{StaticResource AccentEmerald}" StrokeThickness="2.5" StrokeLineJoin="Round"/>
                                                <Polyline x:Name="PolylineCpu" Stroke="{StaticResource AccentPurple}" StrokeThickness="1.5" StrokeLineJoin="Round" StrokeDashArray="4 2"/>
                                                <Polyline x:Name="PolylineGpu" Stroke="{StaticResource AccentAmber}" StrokeThickness="1.5" StrokeLineJoin="Round"/>
                                            </Grid>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                <!-- Hardware Resource Load Breakdown Card -->
                                <Border Background="#182338" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,14">
                                    <StackPanel>
                                        <TextBlock x:Name="TxtHardwareTitle" Text="💻 全系統各硬體元件功耗明細 (Full System Hardware Power Breakdown)" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextMain}" Margin="0,0,0,14"/>

                                        <!-- CPU Row -->
                                        <Border Background="#0C1322" BorderBrush="#1A2438" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="260"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock x:Name="LblHwCpu" Text="🔲 CPU 處理器" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="13"/>
                                                    <TextBlock x:Name="TxtCpuPowerW" Text="~0.0 W" Foreground="{StaticResource AccentCyan}" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <ProgressBar x:Name="PbCpuUsage" Grid.Column="1" Height="10" Value="0" Maximum="100" Foreground="{StaticResource AccentCyan}" Background="#090D16" BorderThickness="0" Margin="10,0" VerticalAlignment="Center"/>
                                                <TextBlock x:Name="TxtCpuUsageVal" Grid.Column="2" Text="0%" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Discrete GPU (dGPU) Row -->
                                        <Border Background="#0C1322" BorderBrush="#1A2438" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="260"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock x:Name="TxtDgpuName" Text="🎮 獨立顯示卡 (dGPU)" Foreground="{StaticResource TextMain}" FontWeight="Bold" FontSize="13" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock x:Name="TxtDgpuPowerW" Text="~0.0 W" Foreground="{StaticResource AccentAmber}" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <ProgressBar x:Name="PbDgpuUsage" Grid.Column="1" Height="10" Value="0" Maximum="100" Foreground="{StaticResource AccentAmber}" Background="#090D16" BorderThickness="0" Margin="10,0" VerticalAlignment="Center"/>
                                                <TextBlock x:Name="TxtDgpuUsageVal" Grid.Column="2" Text="0% (待機省電)" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Integrated GPU (iGPU) Row -->
                                        <Border Background="#0C1322" BorderBrush="#1A2438" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="260"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock x:Name="TxtIgpuName" Text="🖼️ 內建顯示晶片 (iGPU)" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="13" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock x:Name="TxtIgpuPowerW" Text="~0.0 W" Foreground="{StaticResource AccentCyan}" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <ProgressBar x:Name="PbIgpuUsage" Grid.Column="1" Height="10" Value="0" Maximum="100" Foreground="{StaticResource AccentCyan}" Background="#090D16" BorderThickness="0" Margin="10,0" VerticalAlignment="Center"/>
                                                <TextBlock x:Name="TxtIgpuUsageVal" Grid.Column="2" Text="0%" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Screen Display & Backlight Row -->
                                        <Border Background="#0C1322" BorderBrush="#1A2438" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="260"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock x:Name="LblHwScreen" Text="🖥️ 螢幕面板與背光" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="13"/>
                                                    <TextBlock x:Name="TxtScreenPowerW" Text="~0.0 W" Foreground="{StaticResource AccentRose}" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <ProgressBar x:Name="PbScreenBrightness" Grid.Column="1" Height="10" Value="0" Maximum="100" Foreground="{StaticResource AccentRose}" Background="#090D16" BorderThickness="0" Margin="10,0" VerticalAlignment="Center"/>
                                                <TextBlock x:Name="TxtScreenBrightnessVal" Grid.Column="2" Text="75% 亮度" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Wi-Fi Wireless Adapter & Traffic Row -->
                                        <Border Background="#0C1322" BorderBrush="#1A2438" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="260"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock x:Name="LblHwWifi" Text="📶 Wi-Fi / 藍牙網絡" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="13"/>
                                                    <TextBlock x:Name="TxtWifiPowerW" Text="~0.0 W" Foreground="{StaticResource AccentCyan}" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <TextBlock x:Name="TxtWifiSpeedVal" Grid.Column="1" Text="即時傳輸流量: 0 KB/s" Foreground="{StaticResource TextMuted}" FontSize="12" VerticalAlignment="Center" Margin="10,0"/>
                                                <TextBlock Text="無線網卡運作中" Grid.Column="2" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Disk (SSD/HDD) Row -->
                                        <Border Background="#0C1322" BorderBrush="#1A2438" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="260"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock x:Name="LblHwDisk" Text="💾 硬碟 (SSD/HDD)" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="13"/>
                                                    <TextBlock x:Name="TxtDiskPowerW" Text="~0.0 W" Foreground="{StaticResource AccentEmerald}" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <ProgressBar x:Name="PbDiskUsage" Grid.Column="1" Height="10" Value="0" Maximum="100" Foreground="{StaticResource AccentEmerald}" Background="#090D16" BorderThickness="0" Margin="10,0" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                    <TextBlock x:Name="TxtDiskUsageVal" Text="0%" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
                                                    <TextBlock x:Name="TxtDiskStatusText" Text="0 MB/s" Foreground="{StaticResource TextMuted}" FontSize="11" HorizontalAlignment="Right"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>

                                        <!-- RAM Usage & Power Row -->
                                        <Border Background="#0C1322" BorderBrush="#1A2438" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="260"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock x:Name="LblHwRam" Text="🧠 記憶體 (RAM) 匯流排" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="13"/>
                                                    <TextBlock x:Name="TxtRamPowerW" Text="~0.0 W" Foreground="{StaticResource AccentPurple}" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <ProgressBar x:Name="PbRamUsage" Grid.Column="1" Height="10" Value="0" Maximum="100" Foreground="{StaticResource AccentPurple}" Background="#090D16" BorderThickness="0" Margin="10,0" VerticalAlignment="Center"/>
                                                <TextBlock x:Name="TxtRamUsageVal" Grid.Column="2" Text="0 GB / 0 GB" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Motherboard Base Power Row (Fixed Grid.Column="1" for description text) -->
                                        <Border Background="#0C1322" BorderBrush="#1A2438" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="260"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock x:Name="LblHwMotherboard" Text="🔌 主機板與 USB 外設" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="13"/>
                                                    <TextBlock x:Name="TxtMotherboardPowerW" Text="~2.5 W" Foreground="{StaticResource TextDim}" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="基板晶片組與週邊匯流排基礎功耗" Foreground="{StaticResource TextMuted}" FontSize="11" VerticalAlignment="Center" Margin="10,0" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="晶片組運作中" Grid.Column="2" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <Separator Background="#10FFFFFF" Margin="0,10"/>

                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="全系統功耗評級: " Foreground="{StaticResource TextMuted}" FontSize="13"/>
                                            <TextBlock x:Name="TxtPowerLoadStatus" Text="輕度省電" Foreground="{StaticResource AccentEmerald}" FontWeight="Bold" FontSize="13" Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Discrete GPU Hardware Specs List Card -->
                                <Border Background="#182338" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,14">
                                    <StackPanel>
                                        <TextBlock x:Name="TxtGpuListTitle" Text="🎮 系統顯示卡清單 (Graphics Hardware List)" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextMain}" Margin="0,0,0,12"/>

                                        <ItemsControl x:Name="IcGpuList">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="#0C1322" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>

                                                            <StackPanel Grid.Column="0">
                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMain}"/>
                                                                    <Border Margin="10,0,0,0" Padding="8,2" CornerRadius="10" Background="#20F59E0B" BorderBrush="{StaticResource AccentAmber}" BorderThickness="1">
                                                                        <TextBlock Text="{Binding GpuTypeTag}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentAmber}"/>
                                                                    </Border>
                                                                </StackPanel>

                                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                                    <TextBlock Text="專屬視訊記憶體: " Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                                                    <TextBlock Text="{Binding VramText}" Foreground="{StaticResource AccentCyan}" FontWeight="SemiBold" FontSize="12" Margin="4,0,16,0"/>

                                                                    <TextBlock Text="驅動版本: " Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                                                    <TextBlock Text="{Binding DriverVersion}" Foreground="{StaticResource TextMain}" FontSize="12" Margin="4,0,16,0"/>

                                                                    <TextBlock Text="發布日期: " Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                                                    <TextBlock Text="{Binding DriverDate}" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="4,0,0,0"/>
                                                                </StackPanel>
                                                            </StackPanel>

                                                            <TextBlock Grid.Column="1" Text="{Binding Status}" Foreground="{StaticResource AccentEmerald}" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- Live Power Tip Box -->
                                <Border Background="#1038BDF8" BorderBrush="{StaticResource AccentCyan}" BorderThickness="1" CornerRadius="8" Padding="12">
                                    <TextBlock x:Name="TxtLivePowerTip" Text="💡 即時監測每 1 秒自動更新一次 CPU、獨顯、內顯、螢幕背光、Wi-Fi、磁碟與記憶體功耗。" FontSize="12" Foreground="{StaticResource TextMain}" TextWrapping="Wrap"/>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- TAB 2: Live Power & Charge/Discharge History -->
                    <TabItem x:Name="TabHistoryLogs" Header=" 📉 即時功耗與充放電歷史紀錄 ">
                        <Grid Margin="0,12,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Action Buttons Header -->
                            <Grid Grid.Row="0" Margin="0,0,0,10">
                                <TextBlock x:Name="TxtHistoryLogHeader" Text="📋 即時動態功耗與充放電事件歷史日誌 (Live Power &amp; Event Logs)" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextMain}" VerticalAlignment="Center"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="BtnExportPowerCsv" Content="📥 匯出 CSV 紀錄" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Padding="12,6" Click="BtnExportPowerCsv_Click"/>
                                    <Button x:Name="BtnClearPowerHistory" Content="🗑️ 清除紀錄" Style="{StaticResource SecondaryButtonStyle}" Padding="12,6" Click="BtnClearPowerHistory_Click"/>
                                </StackPanel>
                            </Grid>

                            <!-- History Event Log ListView -->
                            <ListView x:Name="LvPowerHistory" Grid.Row="1" Background="#0C1322" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Foreground="{StaticResource TextMain}">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="時間戳記" Width="170" DisplayMemberBinding="{Binding TimestampText}"/>
                                        <GridViewColumn Header="事件類型" Width="130" DisplayMemberBinding="{Binding EventType}"/>
                                        <GridViewColumn Header="放電/充電功率" Width="120" DisplayMemberBinding="{Binding DischargeRateText}"/>
                                        <GridViewColumn Header="電量 (%)" Width="100" DisplayMemberBinding="{Binding BatteryPercentText}"/>
                                        <GridViewColumn Header="CPU 負載" Width="110" DisplayMemberBinding="{Binding CpuUsageText}"/>
                                        <GridViewColumn Header="獨顯負載" Width="110" DisplayMemberBinding="{Binding DgpuUsageText}"/>
                                        <GridViewColumn Header="螢幕功耗" Width="110" DisplayMemberBinding="{Binding ScreenPowerText}"/>
                                        <GridViewColumn Header="詳細狀態記錄" Width="300" DisplayMemberBinding="{Binding SummaryText}"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>
                    </TabItem>

                    <!-- TAB 3: Smart Diagnostics -->
                    <TabItem x:Name="TabDiagnostics" Header=" 💡 智慧診斷與維護建議 ">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                            <ItemsControl x:Name="IcDiagnostics">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#182338" CornerRadius="8" Padding="14" Margin="0,0,0,10" BorderBrush="{StaticResource AccentCyan}" BorderThickness="4,0,0,0">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMain}"/>
                                                <TextBlock Text="{Binding Description}" FontSize="13" Foreground="{StaticResource TextMuted}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>

                    <!-- TAB 4: Life Estimates -->
                    <TabItem x:Name="TabLifeEstimates" Header=" ⏱️ 續航估算表 ">
                        <ListView x:Name="LvLifeEstimates" Background="#0C1322" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Foreground="{StaticResource TextMain}" Margin="0,12,0,0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="記錄期間" Width="220" DisplayMemberBinding="{Binding Period}"/>
                                    <GridViewColumn Header="實測滿電續航" Width="220" DisplayMemberBinding="{Binding FullChargeEstimate}"/>
                                    <GridViewColumn Header="原廠設計續航" Width="220" DisplayMemberBinding="{Binding DesignCapEstimate}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </TabItem>

                    <!-- TAB 5: Recent Usage -->
                    <TabItem x:Name="TabRecentUsage" Header=" 📊 最近使用紀錄 ">
                        <ListView x:Name="LvRecentUsage" Background="#0C1322" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Foreground="{StaticResource TextMain}" Margin="0,12,0,0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="開始時間" Width="220" DisplayMemberBinding="{Binding StartTime}"/>
                                    <GridViewColumn Header="狀態" Width="140" DisplayMemberBinding="{Binding State}"/>
                                    <GridViewColumn Header="電源來源" Width="140" DisplayMemberBinding="{Binding Source}"/>
                                    <GridViewColumn Header="剩餘電量" Width="160" DisplayMemberBinding="{Binding CapacityRemaining}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </TabItem>

                </TabControl>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border x:Name="OverlayLoading" Grid.RowSpan="2" Background="#D0090D16" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⚡" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,14"/>
                <TextBlock Text="正在執行 Windows 電池健康檢測 (powercfg /batteryreport)..." FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextMain}" HorizontalAlignment="Center"/>
                <TextBlock Text="請稍候..." FontSize="13" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
